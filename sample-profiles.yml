jaffle_shop:
  target: dev_gaussdb
  outputs:
    dev_gaussdb: # 华为GaussDB
      type: gaussdbdws # 类型gaussdbdws
      host: 数据库IP地址
      user: 用户名
      password: 密码
      port: 8000  # 默认端口
      dbname: 数据库名称
      schema: Schema名称
      threads: 1
