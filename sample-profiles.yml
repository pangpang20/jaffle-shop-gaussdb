jaffle_shop:
  target: dev_gaussdb
  outputs:
    dev_gaussdb:
      type: gaussdbdws
      host: ${DB_HOST}
      user: ${DB_USER}
      password: ${DB_PASS}
      port: ${DB_PORT}
      dbname: ${DB_NAME}
      schema: ${DB_SCHEMA}
      threads: 1
